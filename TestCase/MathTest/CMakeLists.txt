SET(APPNAME MathTest)

AUX_SOURCE_DIRECTORY(. MathTest_src)

ADD_EXECUTABLE(${APPNAME} ${MathTest_src})

LINK_PROJECT(${APPNAME}
)

TARGET_LINK_LIBRARIES( ${APPNAME} 

  ${PROJECT_SOURCE_DIR}/Lib/${CMAKE_BUILD_TYPE_DIR}/libutility_math.a
  ${CASADI_LIBRARIES}
  ${Boost_LIBRARIES}
  )
